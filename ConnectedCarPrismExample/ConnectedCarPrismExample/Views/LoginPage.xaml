<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="ConnectedCarPrismExample.Views.LoginPage">
    <StackLayout HorizontalOptions="Center" VerticalOptions="Center" Padding="25">
        <Label Text="Usuario" WidthRequest="500"></Label>
        <Entry Text="{Binding UserName}" />
        <Label Text="Password"></Label>
        <Entry Text="{Binding Password}" IsPassword="True"  />
        <Button Text="Ingresar" Command="{Binding LoginCommand}"/>
    </StackLayout>
  
</ContentPage>